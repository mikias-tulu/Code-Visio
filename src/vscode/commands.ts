/** Command ids; must match `contributes.commands` in package.json. */
export const Commands = {
  showGraph: 'codeVisio.showGraph',
  openGraphPanel: 'codeVisio.openGraphPanel',
  refresh: 'codeVisio.refresh',
} as const;
